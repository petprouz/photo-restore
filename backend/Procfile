web: cd backend && export PYTHONPATH=$PYTHONPATH:$(pwd) && poetry install --no-dev && poetry run pip install drf-yasg==1.21.7 && poetry run python manage.py migrate && poetry run gunicorn restore.wsgi:application --bind 0.0.0.0:$PORT 